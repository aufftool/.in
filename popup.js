document.addEventListener('DOMContentLoaded', function() {
    // Create the popup elements
    const popup = document.createElement('div');
    popup.id = 'popup';
    popup.className = 'popup';

    const popupContent = document.createElement('div');
    popupContent.className = 'popup-content';

    const closeBtn = document.createElement('span');
    closeBtn.className = 'close-btn';
    closeBtn.innerHTML = '&times;';

    const title = document.createElement('h2');
    title.textContent = 'Download movie application';

    const message = document.createElement('p');
    message.textContent = 'Click the button below to download the all movie Download application.';

    const downloadLink = document.createElement('a');
    downloadLink.href = 'document.addEventListener('DOMContentLoaded', function() {
    // Create the popup elements
    const popup = document.createElement('div');
    popup.id = 'popup';
    popup.className = 'popup';

    const popupContent = document.createElement('div');
    popupContent.className = 'popup-content';

    const closeBtn = document.createElement('span');
    closeBtn.className = 'close-btn';
    closeBtn.innerHTML = '&times;';

    const title = document.createElement('h2');
    title.textContent = 'Download movie application';

    const message = document.createElement('p');
    message.textContent = 'Click the button below to download the all movie Download application.';

    const downloadLink = document.createElement('a');
    downloadLink.href = 'https://fullmovieu.page.link/tobR';
    downloadLink.download = true;
    downloadLink.className = 'download-link';
    downloadLink.textContent = 'Download Now';

    // Append elements to the popup content
    popupContent.appendChild(closeBtn);
    popupContent.appendChild(title);
    popupContent.appendChild(message);
    popupContent.appendChild(downloadLink);

    // Append the popup content to the popup
    popup.appendChild(popupContent);

    // Append the popup to the body
    document.body.appendChild(popup);

    // Automatically display the popup when the page loads
    popup.style.display = 'block';

    // Close the popup when the close button is clicked
    closeBtn.addEventListener('click', function() {
        popup.style.display = 'none';
    });

    // Close the popup when clicking outside the popup content
    window.addEventListener('click', function(event) {
        if (event.target === popup) {
            popup.style.display = 'none';
        }
    });
});
';
    downloadLink.download = true;
    downloadLink.className = 'download-link';
    downloadLink.textContent = 'Download Now';

    // Append elements to the popup content
    popupContent.appendChild(closeBtn);
    popupContent.appendChild(title);
    popupContent.appendChild(message);
    popupContent.appendChild(downloadLink);

    // Append the popup content to the popup
    popup.appendChild(popupContent);

    // Append the popup to the body
    document.body.appendChild(popup);

    // Automatically display the popup when the page loads
    popup.style.display = 'block';

    // Close the popup when the close button is clicked
    closeBtn.addEventListener('click', function() {
        popup.style.display = 'none';
    });

    // Close the popup when clicking outside the popup content
    window.addEventListener('click', function(event) {
        if (event.target === popup) {
            popup.style.display = 'none';
        }
    });
});
